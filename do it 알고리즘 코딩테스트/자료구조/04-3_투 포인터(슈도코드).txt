N(자연수) 입력받기

사용 변수 초기화(count = 1, start_index = 1, end_index = 1, sum = 1)
            // count가 1인 이유는 자기자신 하나로 이루어진 경우의 수를 미리 저장해둔 것 
while(end_index != N) {
    // 투포인터 이동 원칙
    if(sum == N)
        count 증가, end_index 증가, sum값 변경(+= end_index 값)
    else if(sum > N)
        sum값 변경(-= start_index 값), start_index 증가
    else if(sum < N)
        end_index 증가, sum값 변경(+=end_index 값)
}
count 출력하기
